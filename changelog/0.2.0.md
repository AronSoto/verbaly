# v0.2.0 — Make it adoptable + rich text
Date: 2026-07-03 · Status: **ready to publish** (pending Aron: `npm login` + `pnpm publish`)

## Summary
Turn Verbaly from "core published" into "actually usable end-to-end": first publish of the compiler + Vite plugin + adapters, and rich-text translation (`<Trans>`) in React/Vue. **Aligned versioning: all 5 packages ship at 0.2.0.**

## Scope
- **First publish of `@verbaly/compiler`, `@verbaly/vite`, `@verbaly/react`, `@verbaly/vue`** (the `verbaly` org now exists). Unblocks the whole zero-config flow for real users.
- **`<Trans>` rich-text** in `@verbaly/react` + `@verbaly/vue` — interpolate elements/links inside a translated sentence.
- **npm README** for the `verbaly` core ships (republish core at 0.2.0; adds `packages/core/README.md`).
- Deferred: `verbaly.config.ts`, ICU escape hatch → 0.3.0.

## Design decisions (resolved)
- **`<Trans>` model:** runtime-first — lives entirely in the adapters, no compiler change. It calls the context `t(id, values)`; since the message parser ignores `<`/`>`, the formatted string keeps the tags, which the adapter then interpolates. Compiler write-in-place extraction is a fast-follow.
- **`<Trans>` tag syntax:** named — `Read the <terms>terms</terms>` + `components`.
- **ICU escape hatch:** deferred to 0.3.0 (needs zero-dep-vs-dep design).

## Per package

### @verbaly/react ✅ — `<Trans>` (8 tests)
- `<Trans id values components />`: `components` is `Record<string, ReactElement>`; named tags are wrapped via `cloneElement`. Nesting supported; unknown tags degrade to inner text. Re-renders on locale change (via `useT`). No JSX in src (`createElement`/`cloneElement`).

### @verbaly/vue ✅ — `<Trans>` (10 tests)
- `Trans` component; `components` is `Record<string, (children) => VNodeChild>` (idiomatic Vue render functions). Same parser/behavior as React. Reactive via `trackVersion`.

### verbaly (core)
- No runtime change. Republished at 0.2.0 to ship the npm README (`packages/core/README.md`) and align the ecosystem version. Consumers on `^0.1.0` (e.g. verbaly-web) stay on 0.1.0 — the caret pins the 0.x minor; bump to `^0.2.0` when convenient (no code delta).

### @verbaly/compiler · @verbaly/vite
- No code change. First publish at 0.2.0. Packed manifests verified: `workspace:*` → `0.2.0`, peer `verbaly` → `^0.2.0`.

## Breaking changes
- None.

## New API / signatures
- React: `Trans({ id, values?, components? })`, `TransProps`.
- Vue: `Trans` component, `TransComponents` type.

## Docs impact (for verbaly-web)
- `frameworks` docs page: add a `<Trans>` section (React element map + Vue render-fn map).
- Playground: optional rich-text example.
- If compiler/vite get published, update install/docs to reflect they're installable.

## Tests / verification
- 106 tests total (core 43 · compiler 38 · vite 7 · **react 8** · **vue 10**). Build (tsup), typecheck, lint green. `pnpm install --frozen-lockfile` OK (CI). All 5 tarballs pack at 0.2.0; core includes README.

## Pending for release
- Aron: `npm login` + publish (order: verbaly → compiler → vite → react → vue), all `--access public`.
- After publish: sync docs (verbaly-web `verbaly-docs-sync`) — `<Trans>` section on the `frameworks` page + playground example; update install docs now that compiler/vite/adapters are installable.
